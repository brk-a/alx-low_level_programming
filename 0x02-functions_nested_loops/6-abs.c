#include "holberton.h"

/**
 * _abs - returns the absolute value of an integer
 * @i: i is an integer
 *
 * Return: int
 */

int _abs(int i)
{
if (i >= 0)
{
return (n);
}
else
{
return (-n);  /* Double negative rule in maths */
}
}
