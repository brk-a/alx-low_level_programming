#include "holberton.h"
#include <stdio.h>

/**
 * main - displays name of prog
 * @argc: number of args
 * @argv: array containing args
 * Return: (0)
 */

int main(__attribute__((unused)) int argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
