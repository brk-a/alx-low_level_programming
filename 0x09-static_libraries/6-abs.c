#include "holberton.h"

/**
 * _abs - returns the absolute value of an integer
 * @n: counter
 *
 * Return: int
 */

int _abs(int n)
{
return ((n >= 0) ? n : -n);
/* Double negative rule in maths */
}
